#ifndef FUNCTIONS_H_INCLUDED
#define FUNCTIONS_H_INCLUDED

bool isLegal(const char *buf);
bool isPrintable(const char *buf);
bool addUpNumbers(char input[], char output[] );
void handleErrors(const char *buf);

#endif